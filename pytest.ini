[pytest]
testpaths = tests
addopts = -ra --html=reports/api-test-report.html --self-contained-html --css=tests/report_style.css
markers =
    slow: test exercises the real long-running export workflow